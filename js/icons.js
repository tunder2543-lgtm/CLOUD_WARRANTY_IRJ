/* ============================================================
   icons.js — สร้าง <svg> อ้างอิง sprite ใน index.html
   (ไอคอนเส้นแบบเดียวกันทั้งระบบ แทนอิโมจิ) — โหลดเป็นไฟล์แรก
   ============================================================ */
const ico=(name,cls="")=>`<svg class="ico${cls?" "+cls:""}" aria-hidden="true"><use href="#i-${name}"></use></svg>`;
